{
	"Settings": {
		// SECURITY - findings H-05 (plaintext database credentials) and C-04 (hard-coded encryption
		// key); CWE-798 use of hard-coded credentials, CWE-321 use of a hard-coded cryptographic key;
		// OWASP A02:2021 and A05:2021.
		// THREAT: this file is tracked in source control, so any secret it carries is readable by
		// everyone with repository access, identical across every deployment that does not replace it,
		// and impossible to rotate or revoke.
		// INVARIANT: the secret-bearing values this file declares - ConnectionString, EncryptionKey and
		// EmailSMTPPassword - stay EMPTY in source. Supply them per deployment through environment
		// variables (Settings__ConnectionString, Settings__EncryptionKey, Settings__EmailSMTPPassword)
		// or through user secrets in development; a later provider supplies its value whether or not the
		// key already appears here. The keys are kept with empty values because this JSON source is
		// non-optional and because they record what a deployment has to provide. This host declares no
		// Jwt section, so it carries no bearer-token signing key (finding H-04 applies to the Base and
		// Project hosts, which do). Startup aborts with an actionable message when a required value is
		// absent; see docs/security/secure-configuration.md for the complete list.
		"ConnectionString": "",
		"EncryptionKey": "",
		"Lang": "en",
		"Locale": "en-US",
		"TimeZoneName": "FLE Standard Time",
		"DevelopmentMode": "false",
		"EnableBackgroungJobs": "true",
		"EnableFileSystemStorage": "false",
		"FileSystemStorageFolder": "", // SECURITY: internal UNC storage path removed (CR2-F-12, CWE-200 exposure of sensitive information, CWE-497); blank selects the platform default, supply the deployment path via Settings__FileSystemStorageFolder.
		"EmailEnabled": false,
		"EmailSMTPServerName": "",
		"EmailSMTPPort": "25",
		"EmailSMTPUsername": "",
		"EmailSMTPPassword": "",
		"EmailFrom": "",
		"EmailTo": ""
	},
	"Development": {
		"TestEntityName": "test",
		"TestRecordId": "001ea36f-fd2e-4d1b-b8ee-25d32d4e396c"
	},
	"ApiUrlTemplates": {
		"FieldInlineEdit": "/api/v3/en_US/record/{entityName}/{recordId}"
	}
}